server:
  port: 9101

spring:
  application:
    name: feedback_service
  datasource:
    # 使用阿里的Druid连接池
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver

    url: jdbc:mysql://120.48.72.55:3306/lime
    username: lime
    password: lime

mybatis:
  mapper-locations: classpath:/mapper/*.xml
  check-config-location: true
  type-aliases-package: com.feedback.service.data